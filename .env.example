# Google API 配置
GOOGLE_API_BASE=https://daily-cloudcode-pa.sandbox.googleapis.com

# OAuth 配置（用于刷新 access_token）
OAUTH_CLIENT_ID=1071006060591-tmhssin2h21lcre235vtolojh4g403ep.apps.googleusercontent.com
OAUTH_CLIENT_SECRET=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
OAUTH_TOKEN_URL=https://oauth2.googleapis.com/token

# 项目配置（JSON数组格式）
# 注意：使用 refresh_token，系统会自动刷新 access_token
# 格式: [{"project_id":"项目ID","refresh_token":"刷新令牌"}]
PROJECTS=[{"project_id":"your-project-id","refresh_token":"your-refresh-token"}]

# API Keys（用于验证OpenAI客户端，JSON数组格式）
API_KEYS=["sk-custom-key-1","sk-custom-key-2"]

# 服务配置
HOST=0.0.0.0
PORT=8000

# Token 轮换配置
# 每个 token 使用多少次后切换到下一个（默认 3）
TOKEN_ROTATION_COUNT=3

# Image generation (optional)
IMAGE_DIR=data/images
IMAGE_BASE_URL=
MAX_IMAGES=10
SSE_HEARTBEAT_INTERVAL=15
